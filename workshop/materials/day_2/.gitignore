# Regenerated by the demo scripts (02 and 04) — reproducible, not tracked.
_output/

# Quarto's render cache, if it appears.
/.quarto/

# 05_report.html IS committed on purpose: a pre-rendered fallback the
# facilitator can open if a live render fails. Re-render it with
#   quarto render workshop/materials/day_2/05_report.qmd
